# tvOS Client

This directory is reserved for the Apple TV application target. Reuse modules
from `../Shared/` and keep tvOS focus, remote-control, and playback UI code here.

The tvOS client is not implemented or included in `Package.swift` yet.
