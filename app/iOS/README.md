# iOS Client

This directory is reserved for the iOS application target. Reuse modules from
`../Shared/` and keep UIKit or iOS-specific SwiftUI code within this directory.

The iOS client is not implemented or included in `Package.swift` yet.
